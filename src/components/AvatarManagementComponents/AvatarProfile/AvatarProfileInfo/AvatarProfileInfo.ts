import {Component, Prop, Vue} from 'vue-property-decorator';
import {Method} from 'axios';

@Component({
  name: 'AvatarProfileInfo',
  components: {},
})
export default class AvatarProfileInfo extends Vue {
  // Component Props
  @Prop() readonly showTrendModal: any;
  // Data property

  // Lifecycle hook

  // Component method
}
