import {Component, Vue} from 'vue-property-decorator';

@Component({
    name: 'PublicLayout',
    components: {},
})
export default class PublicLayout extends Vue {
    // Component Props

    // Data property

    // Lifecycle hook

    // Component method
}
