import {Component, Vue} from 'vue-property-decorator';

@Component({
  name: 'Settings',
  components: {},
})
export default class Settings extends Vue {
  // Component Props

  // Data property

  // Lifecycle hook

  // Component method
}
